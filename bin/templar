#! /usr/bin/env ruby -w

require_relative '../lib/templar'

# TODO: Don't really like this.  Should use a flag to get template directory
# before creating a new application.
Templar::Application.new(nil).run
exit

